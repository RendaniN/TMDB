import { MovieArtworkPipe } from './movie-artwork.pipe';

describe('MovieArtworkPipe', () => {
  it('create an instance', () => {
    const pipe = new MovieArtworkPipe();
    expect(pipe).toBeTruthy();
  });
});
